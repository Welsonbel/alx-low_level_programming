#include "main.h"
/**
 * positive_or_negative - le bab checks for n word or p word
 * @i: to chekko
 * Return: always 0
 */

void positive_or_negative(int i)
{
	if (i < 0)
		printf("%d is negative\n", i);
	else
		printf("%d is positive\n", i);
}
