#include <stdio.h>

/**
 * main - this is main the entry point
 *
 * Return: is always 0 yesss
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
